{
  "name": "monad-red-packet",
  "version": "1.0.0",
  "description": "A Web3 red packet DApp on Monad blockchain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy.js --network monad",
    "deploy:local": "hardhat run deploy.js --network localhost",
    "test": "hardhat test",
    "serve": "npx serve .",
    "start": "python3 -m http.server 8000"
  },
  "keywords": [
    "blockchain",
    "web3",
    "monad",
    "red-packet",
    "dapp",
    "solidity",
    "ethereum"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
